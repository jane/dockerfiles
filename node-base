FROM node:14-alpine
LABEL repo="janedev/node-base"
# hadolint ignore=DL3018
RUN apk add --no-cache \
    ca-certificates \
    curl \
    g++ \
    git \
    make \
    python2 \
    wget
ENV TERM xterm-256color

# vim: ft=dockerfile
